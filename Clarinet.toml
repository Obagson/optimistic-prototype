[project]
name = "optimistic-prototype"
authors = ["Your Name <your.email@example.com>"]
description = "A decentralized commitment tracking system with goal verification and staking"
telemetry = false
requirements = []
[contracts.commitment-tracker]
path = "contracts/commitment-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
